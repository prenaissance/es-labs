{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Symlink common",
      "type": "shell",
      "command": "ld -s \"${workspaceFolder}/common\" \"${fileDirname}/common\"",
      "windows": {
        "command": "powershell -Command \"Start-Process cmd -ArgumentList '/c mklink /D ${fileDirname}\\common ${workspaceFolder}\\common' -Verb runAs\""
      },
      "problemMatcher": []
    }
  ]
}
